# This is an assignment from Programming-Hero

The assignment is on javascript. All the code is written in one file, and named as suggested `assignment.js`. And the function names are as followed
* `kilometerToMeter()`
* `budgetCalculator()`
* `hotelCost()`
* `megaFriend()`